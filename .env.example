# ── Backend ──────────────────────────────────────────────
# Required
DATABASE_URL=sqlite:///smart_study_companion.db   # Local: SQLite | Production: postgresql://user:pass@host:5432/dbname
JWT_SECRET_KEY=change-me-to-a-random-string       # Generate with: python -c "import secrets; print(secrets.token_hex(32))"

# AI provider ("openai" or "gemini") — at least one API key required
AI_PROVIDER=gemini
OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional — set in production to your frontend URL(s), comma-separated
# CORS_ORIGINS=https://your-frontend.vercel.app

# Optional — defaults to "development"
# ENVIRONMENT=production

# ── Frontend ─────────────────────────────────────────────
# Set in production to your backend URL
# NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
